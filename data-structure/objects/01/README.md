Напишите функцию, которая принимает аргументом массив, меняет порядок его элементов на обратный и возвращает его.
(Нельзя использовать встроенную функцию reverse)

```
reverse([1, 2, 3]);
> [3, 2, 1]
```