На входе массив из строк в формате CamelCase. Разбейте все строки на пробелы и верните отфильтрованный массив.

```
separating(['daNE', 'getElementById', MobileNetwork])
> ['da ne', 'get element by id', 'mobile network']
```
